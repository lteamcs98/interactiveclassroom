// This module contains simple examples of functions that print a few things on the screen,
// used as examples in the second lecture of cs1.

// Print the current date and time.
function print_date_and_time(){
    var today = new Date();
    console.log(today);
}

// Print the square root of a number.
function print_sqrt(number){
    // Change to print once we have the correct wrapping
    // print(Math.sqrt(number));
    console.log(Math.sqrt(number));
}
